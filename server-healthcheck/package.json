{
  "name": "server-healthcheck",
  "version": "1.0.0",
  "description": "Given the JSON structure belows, build a backend service using TypeScript that  provides the following functionalities via API:",
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npm run clean && tsc",
    "start": "npm run compile && node ./dist/index.js",
    "dev": "nodemon -e ts --exec \"npm run start\"",
    "test": "npx jest",
    "test-watch": "npx jest --watchAll --detectOpenHandles --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.21.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.1",
    "@types/node-fetch": "^2.6.12",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
